# Supabase — Dashboard → Project Settings → API
# Both values are safe in the browser: the anon key only gets what Row Level
# Security allows. Never put the service_role key in this file.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
